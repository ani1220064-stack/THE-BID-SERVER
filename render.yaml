services:
  - type: web
    name: the-bid-server
    runtime: node
    plan: free
    region: oregon
    buildCommand: npm install --production
    startCommand: node src/index.js
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
